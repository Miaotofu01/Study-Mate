# StudyMate · Antigravity 插件

StudyMate 的 Google Antigravity 原生插件包。

包含：
- 5 个专属子代理（位于 `agents/`）：资料收集、采图、课程设计、课件讲解、出题与评估
- 学习总控及规范协议（位于 `skills/`）
- 课件 HTML 渲染与四道严格校验引擎（位于 `scripts/`）
- Sayo UI 与离线 KaTeX 课件模板（位于 `templates/`）
- 意图触发规则（位于 `rules/`）
